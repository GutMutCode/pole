// Example 2: Fibonacci sequence

function fibonacci:
  purpose: calculate n-th fibonacci number
  
  input: 
    - n: integer (which position in sequence)
  
  output: n-th fibonacci number
  
  constraints:
    - n >= 1
    - compute efficiently (avoid redundant calculations)
  
  examples:
    - 1 → 1
    - 2 → 1  
    - 3 → 2
    - 10 → 55
    
  note: 
    - fibonacci sequence: f(1)=1, f(2)=1, f(n)=f(n-1)+f(n-2)
