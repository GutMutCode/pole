@source("examples/16-option-match.pole")

func unwrap_or(opt: Option<Int>, default: Int) -> Int :
  match opt with
  | Some(x) -> x
  | None -> default

@test(unwrap_or(Some(42), 0) == 42)
@test(unwrap_or(None, 99) == 99)
